import React from "react";
import { classNames } from "react-select/dist/declarations/src/utils";
import styled from "styled-components"

interface Props {
    className: string;
}

export const NotificationListModalStyled = styled.div`
  height: 85vh;
  width: 20%;
  background-color: red;
  display: flex;
  margin: 0 500px 0 0;
  position: absolute;

`;
